name: bloc_expenses_tracker
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.8.0
  fl_chart: ^0.66.0
  intl: ^0.19.0
  flutter_colorpicker: ^1.1.0

  # animation
  rive: ^0.13.1
  lottie: ^3.2.0

  expense_repository:
    path: packages/expense_repository
  bloc: ^9.0.0
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7
  uuid: ^4.5.1
  provider: ^6.1.2
  shared_preferences: ^2.5.2 # đang lỗi th này
  google_sign_in: ^6.2.2
  flutter_facebook_auth: ^7.1.1
  
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  flutter_local_notifications: ^19.1.0
  firebase_messaging: ^15.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/
    - assets/lottie/
    - assets/RiveAssets/